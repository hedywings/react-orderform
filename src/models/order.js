// import axios from 'axios';

const initialState = {
  orderList: [],
}

const orderModel =  {
  namespace: 'order',

  /**
   *  Initial state
   */
  state: initialState,

  /**
   * Effects/Actions
   */
  effects: (dispatch) => ({
  }),

  /**
   * Reducers
   */
  reducers: {
  },
};

export default orderModel;